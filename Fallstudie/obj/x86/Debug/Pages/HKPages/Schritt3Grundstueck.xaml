<Page
    x:Class="Fallstudie.Pages.HKPages.Schritt3GrundstückAuswahl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fallstudie.Pages.HKPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="49*" />
            <RowDefinition Height="311*"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding SelectedCustomerr.Lastname}"></TextBlock>
        </StackPanel>

        <StackPanel x:ConnectionId='1' Grid.Row="1" Name="StackSchritt3" Orientation="Horizontal">
            <ListView ItemsSource="{Binding ImagesPlot}" 
                          SelectedItem="{Binding SelectedPlot, Mode=TwoWay}"
                          IsItemClickEnabled="False" SelectionMode="Single"
                          IsSynchronizedWithCurrentItem="False"
                          Height="Auto"
                          >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding SourceImage}" Width="300" Height="200"/>
                            <TextBlock Text="{Binding SourceImage}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button FontSize="36" Content="Upload"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <StackPanel  >
                <TextBlock Text="{Binding SelectedPlot.Preis}"/>
                <Button Margin="20" Command="{Binding ButtonForwardChoosePlot}" FontFamily="Segoe MDL2 Assets" FontSize="36" Content="&#xE0AB;" />
            </StackPanel>


            <StackPanel >
                <ProgressBar Height="26" Margin="10 0 10 10" Value="20"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>

