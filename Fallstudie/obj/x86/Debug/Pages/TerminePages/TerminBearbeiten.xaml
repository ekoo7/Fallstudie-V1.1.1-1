<Page
    x:Class="Fallstudie.Pages.TerminePages.TerminBearbeiten"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fallstudie.Pages.TerminePages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Converter="using:Fallstudie.Converter"
    DataContext="{Binding Main, Source={StaticResource Locator}}"
    mc:Ignorable="d">
    <Page.Resources>
        <Converter:DateTimeConverter x:Key="DateConverter"/>
        <Converter:TimeSpanConverter x:Key="TimeConverter"/>
    </Page.Resources>

    <ScrollViewer>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Termin bearbeiten"></TextBlock>
                <TextBlock Text="{Binding SelectedAppointment.Customer.Lastname}" Foreground="Red"/>
                <TextBlock Text="{Binding SelectedAppointment.Consultant.Lastname}" Foreground="Red"/>
                <TextBlock Text="{Binding SelectedAppointment.DateFormat}"  Foreground="Red"/>
                <TextBlock Text="{Binding SelectedAppointment.Time}" Foreground="Red"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Kunde" Margin="10"/>
                    <ComboBox Width="180" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomerr, Mode=TwoWay}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Lastname}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <!--Consultant-->
                    <TextBlock Text="Mitarbeiter" Margin="10"/>
                    <ComboBox Width="180" ItemsSource="{Binding Consultants}" SelectedItem="{Binding SelectedConsultant, Mode=TwoWay}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Lastname}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>




                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <DatePicker MinYear="{Binding DateTimeNow, Mode=TwoWay, Converter={StaticResource DateConverter}}" Date="{Binding DateAppointment, Mode=TwoWay, Converter={StaticResource DateConverter}}" VerticalAlignment="Top"/>
                    <TimePicker Time="{Binding TimeAppoitment, Mode=TwoWay}" VerticalAlignment="Top" ClockIdentifier="24HourClock"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>
    <Page.BottomAppBar>
        <CommandBar HorizontalContentAlignment="Left">
            <CommandBar.Content>
                <Grid/>
            </CommandBar.Content>
            <AppBarButton Icon="Back" Command="{Binding ButtonBackToAppointmentPage}" Label="Zurück" />
            <AppBarButton Icon="Save" Command="{Binding ButtonSaveEditedAppointment}" Label="Speichern" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>

