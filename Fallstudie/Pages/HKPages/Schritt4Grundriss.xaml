<Page
    x:Class="Fallstudie.Pages.HKPages.Schritt4Grundriss"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fallstudie.Pages.HKPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="49*" />
            <RowDefinition Height="311*"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding SelectedCustomerr.Lastname}"></TextBlock>
            
        </StackPanel>

        <StackPanel Grid.Row="1" Name="StackSchritt4" Orientation="Horizontal">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Anzahl der Stockwerke"></TextBlock>
            
            
                <ComboBox ItemsSource="{Binding NumberFloors}" SelectedItem="{Binding SelectedItemFloor, Mode=TwoWay}" />
            </StackPanel>
            <ListView ItemsSource="{Binding FloorsGroundPlot}" 
                          SelectedItem="{Binding SelectedFloor, Mode=TwoWay}"
                          IsItemClickEnabled="False" SelectionMode="Single"
                          IsSynchronizedWithCurrentItem="False"
                          Height="Auto"
                          >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Description}"></TextBlock>
                            <Image Source="{Binding SourceImage}" Width="300" Height="200"/>
                            <Button Content="Umriss zeichnen" Command="{Binding ButtonDrawSketch}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <StackPanel  >
                <TextBlock Text="{Binding SelectedFloor.Price, Mode=TwoWay}"/>
                <TextBlock Text="{Binding TotalPrice}"/>

                <Button Margin="20" Command="{Binding ButtonForwardChooseWall}" FontFamily="Segoe MDL2 Assets" FontSize="36" Content="&#xE0AB;" />
            </StackPanel>


            <StackPanel >
                <ProgressBar Height="26" Margin="10 0 10 10" Value="30"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
